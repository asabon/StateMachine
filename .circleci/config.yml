version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: cd test/googletest-release-1.10.0/; mkdir buikd; cd build; cmake ..; make
      - run: cd test; make
